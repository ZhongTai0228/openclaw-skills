# 2026-03-02

## 公众号排版 - YC Agent Economy 文章

### 关键发现
- **`text-align: justify` 必须写在 `<p>` 上**，写在 `<span>` 上完全无效（CSS 规范：text-align 只对块级元素生效）
- `word-break: break-all` 太暴力，会从英文单词中间劈开
- `overflow-wrap: break-word` 也不对，不是根本解决方案
- 正确做法：只在 `<p>` 上写 `text-align: justify`，不加任何额外断词属性，浏览器默认行为就是最佳效果

### 图片提取方法
- `fetch()` + `credentials: 'include'` 从飞书页面同源请求 = 稳定可靠
- `new Image()` + `crossOrigin='use-credentials'` + canvas = CORS 失败，废弃
- `feishu_drive_fetch_media` = 缺 app scope，不可用

### 公众号编辑器
- `#title` 是 `<textarea>`，必须用 native setter 设置
- 不设标题会导致 `default-203` 保存错误
- 预览发送不受保存失败影响（独立操作）

### 排版规则更新
- 最后三张图 margin-bottom: 8px（紧凑排列）
- 已更新 skill `agentverse-wechat` 和 style-guide

## agentverse-wechat Skill 创建

### Skill 结构
- `skills/agentverse-wechat/SKILL.md` — 完整 8 步 pipeline
- `skills/agentverse-wechat/references/style-guide.md` — 观猹笔记排版规范
- `skills/agentverse-wechat/scripts/extract_images.js` — CDP 图片提取
- `skills/agentverse-wechat/scripts/fix_typography.py` — 引号+中英文间距
- Git commit: `66656c7` on main

### Skill 创建反思
- 不应该只跑一篇就写 skill，至少跑两篇验证后再总结
- Gotchas 应该事前预判而非事后补
- 排版调试应先本地确认再走完整粘贴流程

### 用户偏好（待写入 USER.md）
- 中文沟通，直接风格，不喜欢废话
- 排版审美：简洁克制（观猹笔记风格）
- 习惯快速迭代，凌晨工作
- 希望我遇到问题先思考再动手，不要急着执行

## GitHub 备份设置
- 已安装 `gh` CLI (v2.87.3)
- 正在进行 `gh auth login` — 验证码 AA36-13ED
- 等待用户在 github.com/login/device 完成授权
- 计划创建私有仓库 `openclaw-workspace` 并 push
- **今后 skill 改动都要 git commit**

## Session 保存策略
- 探索型/重大踩坑的 session → 保存到 `memory/sessions/`
- 常规执行型 session → 不保存
- 关键信息始终写进 memory 和 skill，不依赖原始 session
- 本次 session 已保存: `memory/sessions/2026-03-02-agentverse-wechat.jsonl`
